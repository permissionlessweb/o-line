# Playbook: Resusable Tools & Scripts For Terp Network

- we have justfiles for reusable devops & cicd commmands
- the purpose of this playbook is for ease of use without sacraficing features
- use `cargo chec` & `cargo tes` in direct replace of `cargo check` and `cargo test`. we can still use all flags and techniquest, thesea re just subcommand wrappers to output response in optimized formats.