# ─────────────────────────────────────────────────────────────
# O-Line Deployer — Environment Variable Defaults
# ── Encrypted Mnemonic (written by `oline encrypt`) ──

# ── Runtime Defaults ──
# Directory containing SDL template files (default: sdls/)
# OLINE_SDL_DIR=sdls

# Env var name for the encrypted mnemonic (default: OLINE_ENCRYPTED_MNEMONIC)
# OLINE_ENV_KEY_NAME=OLINE_ENCRYPTED_MNEMONIC

# Comma-separated list of env var names to mask when printing
# OLINE_SECRET_KEYS=S3_KEY,S3_SECRET,MINIO_ROOT_USER,MINIO_ROOT_PASSWORD,TERPD_P2P_PRIVATE_PEER_IDS,CF_API_TOKEN

# ── Akash Network Endpoints ──
OLINE_RPC_ENDPOINT=https://rpc.akashnet.net:443
OLINE_GRPC_ENDPOINT=https://grpc.akashnet.net:443

# ── Snapshot Bootstrap ──
# URL to fetch the latest snapshot state JSON (auto-detected filename)
OLINE_SNAPSHOT_STATE_URL=https://server-4.itrocket.net/mainnet/terp/.current_state.json
# Base URL prepended to the snapshot filename from the state JSON
OLINE_SNAPSHOT_BASE_URL=https://server-4.itrocket.net/mainnet/terp/
# Direct snapshot URL (overrides auto-fetch if set)
OLINE_SNAPSHOT_URL=

# ── Validator ──
OLINE_VALIDATOR_PEER_ID=
OLINE_TRUSTED_PROVIDERS=
OLINE_AUTO_SELECT_PROVIDER=n

# ── SDL Template Defaults ──
# Cosmos Omnibus container image used by all chain nodes
OLINE_OMNIBUS_IMAGE=ghcr.io/akash-network/cosmos-omnibus:v1.2.37-generic
OLINE_CHAIN_JSON=https://raw.githubusercontent.com/permissionlessweb/chain-registry/refs/heads/chore/fix-terpnetwork-node_home/terpnetwork/chain.json
OLINE_ADDRBOOK_URL=https://raw.githubusercontent.com/111STAVR111/props/main/Terp/addrbook.json

# ── Snapshot Export (S3) ──
OLINE_SNAPSHOT_PATH=snapshots/terpnetwork
OLINE_SNAPSHOT_TIME=00:00:00
OLINE_SNAPSHOT_SAVE_FORMAT=tar.gz
OLINE_SNAPSHOT_RETAIN=2 days
OLINE_SNAPSHOT_KEEP_LAST=2

# ── MinIO-IPFS Service ──
OLINE_MINIO_IPFS_IMAGE=ghcr.io/permissionlessweb/minio-ipfs:v0.0.2
OLINE_S3_BUCKET=terp-snapshots
OLINE_AUTOPIN_INTERVAL=300
OLINE_SNAPSHOT_DOWNLOAD_DOMAIN=snapshots.terp.network

# ── Cloudflare DNS (optional) ──
# Auto-update CNAME records after Phase A deployment.
# Requires a Cloudflare API token with DNS:Edit permission for the zone.
# OLINE_CF_API_TOKEN=
# OLINE_CF_ZONE_ID=

# ── S3 Test (`oline test-s3`) ──
# These are NOT used during deployment (credentials are auto-generated).
# Set these to test connectivity to a running MinIO instance.
# S3_KEY=
# S3_SECRET=
# S3_HOST=
# SNAPSHOT_PATH=snapshots/terpnetwork
