{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "output": {
      "type": "string",
      "description": "Path to write deploy-config.json",
      "default": "deploy-config.json"
    },
    "template": {
      "type": "string",
      "description": "Use a named template for non-interactive config generation. All FIELD_DESCRIPTOR defaults are applied; template overrides are layered on top. Run with --list-templates to see available names"
    },
    "list_templates": {
      "type": "boolean",
      "description": "Print available template names and exit"
    }
  },
  "required": []
}
