{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "output": {
      "type": "string",
      "description": "Write rendered SDL files and deploy-config.json to this directory"
    },
    "load_config": {
      "type": "string",
      "description": "Load a deploy-config.json instead of prompting for values"
    }
  },
  "required": []
}
